<hibernate-configuration>

    <session-factory>
        <property name="connection.driver_class" value="${jdbc.driverClassName}"/>
        <property name="connection.url" value="${jdbc.url}"/>
        <property name="connection.username" value="${jdbc.username}"/>
        <property name="connection.password" value="${jdbc.password}"/>
        <property name="connection.pool_size">1</property>
        <property name="current_session_context_class">thread</property>
        <property name="show_sql">true</property>
        <property name="dialect">org.hibernate.dialect.PostgreSQL9Dialect</property>

    </session-factory>

</hibernate-configuration>